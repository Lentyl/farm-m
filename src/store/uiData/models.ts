class Product {
    title: string;
    price: number;
    capacity: number;
    constructor(title: string, price: number, capacity: number) {
        this.title = title;
        this.price = price;
        this.capacity = capacity;

    }
}

export default Product;